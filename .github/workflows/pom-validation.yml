name: 🧪 Validate POM & Maven Dependencies

on:
  pull_request:
    paths:
      - 'pom.xml'
      - 'scripts/validate-pom.sh'
  push:
    branches:
      - main
      - dev

jobs:
  validate-pom:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'maven'

      - name: 🔒 Set execute permission on script
        run: chmod +x scripts/validate-pom.sh

      - name: 🚀 Run custom POM validator script
        run: ./scripts/validate-pom.sh

      - name: 📦 Validate Maven dependencies
        run: mvn dependency:resolve

      - name: ✅ Run Maven validate phase
        run: mvn validate
